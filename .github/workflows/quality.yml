name: Quality

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Show customer Prettier diff
        run: |
          npx prettier --write src/App.test.tsx src/App.tsx src/components/AppComponents.tsx src/features/customer/CustomerScreens.tsx
          git diff -- src/App.test.tsx src/App.tsx src/components/AppComponents.tsx src/features/customer/CustomerScreens.tsx
          exit 1
      - run: npm run check
      - run: npm run format:check
